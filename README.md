### To build and run the containers in daemon mode:

> Run: docker-compose up -d --build

Now, access "[localhost:5000](http://localhost:5000)" on the browser.

### Stop the containers:

> Run: **docker-compose down**
